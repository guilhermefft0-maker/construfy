{
  "name": "construfy",
  "version": "1.0.0",
  "description": "ConstruFY — Plataforma SaaS de Gestão de Construtoras (Cloudflare Workers + D1)",
  "scripts": {
    "deploy:db": "wrangler d1 execute construfy-db --file=./backend/schema.sql --local=false && wrangler d1 execute construfy-db --file=./backend/sample-data.sql --local=false",
    "deploy:worker": "wrangler deploy ./backend/worker.js --name construfy-worker",
    "deploy": "npm run deploy:db && npm run deploy:worker",
    "dev": "wrangler dev ./backend/worker.js --remote",
    "db:shell": "wrangler d1 execute construfy-db --command --local=false",
    "secret:jwt": "wrangler secret put JWT_SECRET",
    "secret:resend": "wrangler secret put RESEND_API_KEY"
  },
  "devDependencies": {
    "wrangler": "^3.57.4"
  },
  "keywords": ["construfy", "saas", "construcao", "workers", "d1"],
  "author": "BLACKBOXAI",
  "license": "MIT"
}
